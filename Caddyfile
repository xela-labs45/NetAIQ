{$SITE_ADDRESS} {
	# Caddy automatically uses its internal CA for 'localhost' and local IP addresses.
	# For public domains, it will automatically use Let's Encrypt or ZeroSSL.
	
	reverse_proxy netmon:3001
}
