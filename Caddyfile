{
	debug
}

# Primary listener for the IP address
{$SITE_ADDRESS} {
	tls internal
	reverse_proxy netmon:3001
}

# Secondary listeners for loopback
localhost, 127.0.0.1 {
	tls internal
	reverse_proxy netmon:3001
}
