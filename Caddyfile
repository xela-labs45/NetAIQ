{$SITE_ADDRESS}, localhost, 127.0.0.1 {
	# Explicitly use internal TLS for local addresses.
	# Caddy will automatically generate and trust a self-signed cert for these.
	tls internal

	reverse_proxy netmon:3001
}
